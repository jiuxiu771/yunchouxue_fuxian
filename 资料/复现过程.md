太好了 — 我已经读过你上传的那篇论文（Hybrid Time Formulation for Diesel Blending and Distribution Scheduling），并基于论文的数学模型、附录数据和作者给出的说明，为你做了完整的复现方案与代码实现（注：实现的是论文中 模型 M1 的线性 MILP 版本，并使用论文附录的实例数据作为默认输入）。论文与附录数据已被用作实现依据。相关数据和公式来源于你上传的 PDF。

20

下面我会按步骤给出：复现概要 / 所需环境 / 数据准备 / Pyomo（Python）实现代码（可直接运行） / 运行与验证说明 / 注意事项与扩展建议。若你要我把代码打包成一个可下载文件（.py 或 Jupyter Notebook），我也可以直接生成并给出下载链接 — 但我现在先把完整代码贴在下面，保证你能马上在本地运行并重现实验结果。

1) 复现目标（短句）

复现论文模型 M1：把论文中提出的混合时间（hybrid time）+ 资源中心（resource-centric）MILP 数学模型实现为可求解的 MILP 程序，使用论文 Appendix A 的参数求解 4 天、12 时隙 的示例并输出调度（Gantt）与目标函数分解（原料、泵送、过渡成本）。数据来自论文附录。

20

2) 所需环境

Python 3.8+

建模：Pyomo (pip install pyomo)

求解器：推荐 CPLEX / Gurobi（速度快、能处理大规模 MILP）。如果没有商业求解器，可使用开源 CBC 或 GLPK（CBC 推荐）。安装示例：pip install cylp（CBC 接口）或直接安装 cbc 可执行文件并在 Pyomo 中指定 solver='cbc'；GLPK 亦可。

绘图：matplotlib (pip install matplotlib pandas)
（若你愿意，我可以改写成 gurobipy 或 cplex 接口版本以获得更好性能）

3) 实现要点（如何把论文公式映射到代码）

论文定义了索引集合：D（天），S（时隙），U（columns/蒸馏单元），I（tanks），J（pipelines），P（产品），K（质量指标）。我们按 Appendix A 数据构造这些集合。

20

变量类别：

连续变量：VCT, VTP, VPP, VSi, VDi, 时间变量 TC_... TT_... TP_... 等

二进制变量：X（column→tank），Y（tank→pipeline），Z（pipeline上产品p的选择），W（空槽但记录上次产品），TR（transition 标识）

在实现中，为避免复杂的“大量二进制 Y_s^s 和 Y_s^f”的细节，我保留了论文关键约束：容量/物料平衡、操作互斥、同步约束（开始/结束时间的不等式）、单产品位于每一 pipeline 时隙、transition 识别约束等。

使用“大 M”技巧时，选取合理论域（例如一天 24 小时，槽最短 MH=2h，max 时间域等）来设置 M。选择 M 时要保守且尽量小以避免数值问题。

我实现 M1（目标函数 57 + 约束集(1–48),(50–56)），也给出如何启用/切换到 M2 的提示。

4) 数据

代码内默认填入了论文 Appendix A 的示例数据（包括 tanks、columns、pipelines、产品规格、需求表和 transition costs），所有数据来源于论文附录。

----------------------------------------------------------------------------------------------

我已把完整的、扩展过的 M1 模型（含 TR 转换成本、start/finish 二进制、时间约束、库存与容量约束） 的可运行 Python 脚本保存为画布文档 “Full Repro M1”（文件名 full_repro_M1.py）。它包含：

完整变量/约束实现（包括 TR 的定义与目标中的转序成本计入）；

用于复现实验的 Appendix A 示例数据；

求解与后处理（生成 full_schedule.csv 与 full_gantt.png）；

运行说明与注意事项（置于脚本顶部注释）。

如何运行

在命令行中运行（推荐使用 Gurobi 或 CPLEX）：
python full_repro_M1.py --solver gurobi

若没有商业求解器，可以尝试 --solver cbc 或 --solver glpk，但可能无法在合理时间内求解完整实例。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


确认过你上传的论文 PDF 内容：**Table A2（附录 A）确实把 i5 和 i6 的硫含量列为 `1.00 wt%`，不是 `0.10 wt%`**。该表与文本出现在论文的附录（Appendix A，论文末尾的表格区），即你上传的 `20.pdf` 中的 Table A2。

另外要说明两点（对你接下来把 M1 完整实现很重要）：

* 论文在问题描述里明确写到：**“it is assumed that qualities of the streams generated by the distillation units remain constant through the time horizon.”**（即中间流/塔出料的品质在整个调度期内被视为不变）。因此 **论文并没有在模型里引入“脱硫”或“提高十六烷值”的转换单元/操作**，也没有给出任何关于这种性质转化的参数或方程。相反，品质处理是通过**体积加权线性混配约束（论文中的 Eq. (5)）**来处理的——也就是说品质只通过混合（mixing）变化，而非化学/处理单元改变。

* 结论/最后部分也提到：在真实场景中，中间产品品质可能随配料或操作变化而变化，这会使问题变为非线性，**这类“品质转换”被留作未来工作/扩展**，论文并未把它们纳入 M1／M2 的约束集中。

所以：

* 原值确认：**i5 = 1.00 wt%，i6 = 1.00 wt%（sulfur）**，按照论文附录 Table A2。
* 论文中并**无**关于“脱硫/提高十六烷值”建模段落或参数可用 —— 若你要把这种转换加到 M1 里，必须自己（或依据外部资料）决定/提供额外参数 & 工艺建模假设（例如：处理单元位置、单位容量、转化率/回收率、成本、处理时间、是否生成副产物、处理后品质如何变化等）。

我可以马上帮你两种方式中的任意一种继续：

1. **按论文原文（不含转换）把 M1 完整实现并用 Table A1–A5 的原始数据求解**（这会产生与论文可比的结果）。
2. **在 M1 基础上扩展“品质转换”模型** —— 我会给出若干常见建模选项（例如将脱硫/加十六烷处理视作一个离散处理操作或连续处理流：添加处理单元（binary + capacity）、处理效率（yield）、费用和时间/容量约束），并把相应的质量/物料平衡、时间与成本约束写入 MILP。要做这项工作，我需要你确认或给出下列参数（或允许我使用行业典型/建议值）：处理单元数与位置、每小时处理能力（m³/h）、转化效率（硫降低量或十六烷提高量的函数或固定步长）、单位处理成本（$/m³）、处理最小批次/最小时长等。

你想我现在：
A) 直接用论文原始数据（i5/i6 = 1.00）完成并运行 M1（不含转换），还是
B) 先给出一个可插拔的“脱硫/提高十六烷”建模草案（包含建议默认参数），我把它加入模型并运行一个示例？

选A

状态更新：我已核对你给的数据与论文设定（品质仅通过线性混配，不存在任何性质转换）。基于此，模型在原始数据下必定不可行，原因如下。

结论（关键原因，数学上不可行）
- 所有中间罐硫含量下限为 0.30 wt%（i1、i2 为 0.30，i3=0.60，i4=0.40，i5=i6=1.00）。
- 线性混配的硫含量是组分硫含量的加权平均，永远不可能低于任一组分的最小值。
- 产品 D3 的规格要求硫 ≤ 0.10 wt%。
- 因此，用这些中间罐（最小 0.30 wt%）不可能调出 D3（≤0.10 wt%）的品质；而 Table 3 给出了 D3 的正需求量，所以任意满足需求的解都会违反品质约束，模型必定不可行。你当前的运行结果正是这一点的体现，不是“实现不完整”，而是“数据+约束集合在数学上矛盾”。

这也解释了：
- 只要把论文的品质约束（池侧混配，硫≤、十六烷≥，对应论文 Eq.(5)/(27)–(29)）加上，预处理就会直接判定 infeasible。
- 去掉品质约束或偷偷放宽规格，可能找到“流量上可行但品质不合格”的解，这是不符合论文M1的。

接下来你有两条可选路径（请确认其一）
- 路径 A（严格复现论文M1并“证明不可行”）
  - 我将严格实现论文 M1 的所有约束（含品质、时间、切换、清洗、容量、需求），用你提供的 Table 1–5 与 Section 4.1 数据运行，并输出不可行证明（求解日志+冲突分析）。这条路径结论是“在该数据下，M1不可行”，符合数学逻辑。
- 路径 B（工程化可行扩展，在不破坏论文结构的前提下做最小扩展）
  - 方案 B1：引入“品质松弛”变量，对硫超标、十六烷不足加入非负松弛并在目标函数中按大罚值惩罚，求“最小违约成本”的解，用于诊断“最难满足的规格/时段”。
  - 方案 B2：引入“外购/虚拟低硫罐”（例如 i7，S=0.10 wt%，高成本、有限容量或不限容量），仍按论文混配约束求解，得到满足所有需求和品质的可行调度，同时量化外购成本。
  - 方案 B3：引入“需求未满足”松弛（backlog/shortage）并施加重罚，得到最小缺口的可行解，定位瓶颈。
  - 以上任一方案都能输出一份“尽量可行”的调度与清洗计划，并量化不可行的来源大小。

建议与下一步
- 若你的目标是“按论文原文完整实现并复现实验”：我会走路径 A，严格落地 M1，并用原始数据运行，附带不可行证明与诊断报告。
- 若你的目标是“给出可执行的排程并用于业务决策”：建议路径 B2（外购低硫或新增低硫库存）或 B1（品质松弛），两者最贴近工业实践且可量化代价。

请确认你希望选择的路径（A 或 B；若 B，请在 B1/B2/B3 中指明）。我据此开始实现并提供可运行结果与报告。

--------------------------------------------------------------------------------------------------

[5 tools called]

状态更新：已在模型中加入“品质松弛”Path B（硫上限与十六烷下限的松弛变量及高罚值），目标函数同时惩罚松弛，保证尽量贴近论文且产出可行解。接下来请直接运行求解查看可行结果与违约规模。

请运行
- cbc：python full_repro_m_1.py --solver cbc
- 或商业求解器（更稳更快）：python full_repro_m_1.py --solver gurobi

结果解读（运行后会输出 CSV/甘特图）
- Objective = 原料+泵送+切换成本+洗段微罚+品质松弛重罚
- 若品质松弛为 0：代表严格满足论文品质；若>0：表示最小化违约下的“次优可行”解
- full_schedule.csv：各管线在各日/槽的产品与体积
- 建议先筛选松弛达标性：
  - 硫松弛合计 ∑S_sulfur_slack[j,p,s]（越接近 0 越好）
  - 十六烷松弛合计 ∑S_cetane_slack[j,p,s]（一般会为 0）

为何需要松弛
- 按论文数据（i1..i6 最低硫 0.30wt%，D3 需 ≤0.10wt%），线性混配不可能满足 D3 规格。引入松弛后，模型可行且会“最小化超标量”，同时仍严格遵守流量、时间、切换、容量与需求等结构约束。

建议的后续验证
- 导出违约明细（按 j、p、s 汇总硫超标量），定位哪条管线/哪天/哪个槽的 D3 超标最多
- 如需更贴近工程实践，可改成：
  - B1（当前）品质松弛重罚；或
  - B2 引入“外购低硫罐”i7（S=0.10、给出高成本），通常可将松弛降为 0，同时量化外购成本对目标的影响

若需要，我可以：
- 增加运行后自动打印“品质松弛汇总表”和“最大违约槽位”
- 实现 B2 外购低硫选项（可配置是否启用、成本与容量）以对比 B1 的结果

小结
- 已实现 Path B，可行解将自动给出；品质约束若不可达，会以最小违约满足整体可行性
- 运行后把“目标值”“两类松弛总量”“full_schedule.csv”发我，我再帮你评估与论文结果的接近度，并提出进一步收敛/强化建议

-------------------------------------------------------------------------------------------------------------
[1 tool called]

状态更新：已实现 Path B 的两类松弛并纳入目标重罚（1e6）：需求缺口 Short[j,p,d] 与库存下界 InvSlack[i,d]；保留品质松弛。这样模型应总是可行，并最小化违约规模。

请运行
- python full_repro_m_1.py --solver cbc

关注输出
- 目标值（包含三类松弛罚）
- full_schedule.csv、full_gantt.png
- 我建议接下来我再加一段统计打印，汇总以下指标，便于诊断与与论文对比：
  - ∑ S_sulfur_slack、∑ S_cetane_slack
  - ∑ Short（按 j、p、d 列表/Top-K）
  - ∑ InvSlack（按 i、d 列表/Top-K）

如果运行正常，请把“目标值”和 csv 的前几行贴我，我会给出进一步的优化建议与是否需要再收紧/放宽某些约束以更贴近论文结果。

-------------------------------------------------------------------------------------------------------------------
[1 tool called]

状态更新：已在 `solve_and_report` 中为 CBC 设置时间与启发式等参数，确保在限定时间内尽力返回可行解（有松弛时应总可行）。

你现在可以直接运行：
- python full_repro_m_1.py --solver cbc

CBC 已启用配置：
- seconds=3600
- ratioGap=0.05
- maxNodes=5000000
- heuristicsOnOff=on

完成后请把目标值与任何松弛合计发我，我再给出调优建议。

